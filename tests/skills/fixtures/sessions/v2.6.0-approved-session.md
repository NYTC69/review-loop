<!--
Synthetic v2.6.0 fixture — stable across machines; SHA placeholders are intentional.

Shape: a fully-valid v2.6.0 session ready for `execute --session`:
- `## Current Phase: execution`
- `## Approved Plan` populated with `- Source: reviewer-approved`
- `## Session Metadata` carries entry_point, plan_source, and the full
  baseline quintet plus completed_stages / delivery_blocked_by.

Smoke tests that seed this fixture drive `execute --session <uuid>` without
needing `--accept-external-state`; the baseline quintet is intentionally
set to placeholder SHAs so the fixture is stable across machines — the
smoke runner does not verify the fixture against live git state, and the
drift-check's decision tree still reaches step 4 safely on drift (which
seeding code is expected to normalize via copy).
-->

## Problem Description
Add a synthetic v2.6.0 session fixture that `execute --session` can resume.

## Context
- The fixture is synthetic and intentionally compact.
- It mirrors the v2.6.0 session schema enforced by `docs/protocol/session-file.md`.
- Stable across machines: SHA placeholders are intentional and not replaced on copy.

## Acceptance Criteria
- The session file contains all canonical sections in the required order.
- The approved plan is populated with `- Source: reviewer-approved`.
- `## Session Metadata` is the final section and carries the full v2.6.0 quintet.

## Current Phase
execution

## Approved Plan

- Source: reviewer-approved

### Solution Plan: Stable v2.6.0 fixture
1. Land the fixture at `tests/skills/fixtures/sessions/v2.6.0-approved-session.md`.
2. Reuse the fixture from smoke cases that seed `execute --session`.

## Review History

### Planning Round 1
- Executor backend: `general-purpose` (inlined)
- Executor result: proposed the v2.6.0 fixture shape above.
- Reviewer backend: `codex`
- Reviewer verdict: `APPROVE`
- Reviewer issues:
  - None.
- Outcome: Approved for fixture creation.

## Files Changed
None.

## Key Related Files
- `docs/protocol/session-file.md`
- `tests/skills/smoke/execute.session-resume.smoke.claude.json`
- `tests/skills/smoke/execute.stop-after-polish.smoke.claude.json`

## Timing Log
| Phase | Round | Role | Duration |
|-------|-------|------|----------|

## Session Metadata
- entry_point: plan
- plan_source: reviewer-approved
- base_head: 0000000000000000000000000000000000000000
- base_dirty: {}
- last_verified_head: 0000000000000000000000000000000000000000
- last_verified_dirty: {}
- session_commits: []
- completed_stages: []
- delivery_blocked_by: null
