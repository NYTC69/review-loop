**Last updated**: 2026-05-07 (v2.6.25 close + 2 P3 follow-ups)

## P0 — blocker / must-do-now

## P1 — high priority

~~Debug Claude review stall in manual / review-loop review flows.~~ (closed 2026-04-26 — root cause: `--output-format json` buffers all output until generation ends; sonnet-4-6 with extended thinking on 70k cached tokens takes 2-3 min, producing no visible output. With `--include-partial-messages` the first thinking_delta arrives at ~3.7s confirming the process is alive. Fix: switched Codex Stage 1 reviewer command to `--output-format stream-json --include-partial-messages`; orchestrator now scans line-by-line for `type == "result"` event. Updated SKILL.md, docs/protocol/planning.md, CLAUDE.md, and contract test needle.)

## P2 — normal

~~[new] Add a dry-run Orchestrator mode that executes /review-loop against a fixture repo and validates the Agent-call sequence without writing files, so sandbox/agent-type bugs are caught pre-merge instead of via live tool_uses: 0 symptoms. (added 2026-04-19)~~ (closed 2026-05-02 — refined to lint+smoke assertions per [ADR-1](DECISIONS.md). No orchestrator process exists to dry-run; SKILL.md IS the orchestrator-as-prose. Replaced with 4-gap concrete scope: lint kinds A1 `command_flag_co_occurrence` + A2 `agent_subagent_type_whitelist`, smoke kinds B3 `tool_use_min_count` + B4 `tool_use_agent_subagent_type_whitelist`. Plan approved over 4 codex-reviewed rounds; see `.review-loop/sessions/b1e5ecca-6bc1-43cb-b6d3-c8b5174e60ca.md`. Implementation pending via `review-loop:execute --session b1e5ecca-...`.)
- [new] Bring back a conflict-aware parallel CR scheduler (`scripts/review_verification.py` + wrapper + tests). The pruned `feat/lint-agent-type-context-guard` branch (ref via git reflog) prototyped one in mid-April: a per-job scheduler that runs review-loop CRs in parallel under a strict per-job timeout, with returncode propagation, drain-on-timeout, and immutability of finished jobs. Useful when one orchestrator wants to fan out N independent reviewer rounds without serial wall-clock cost. Current main has no such scheduler — every CR is sequential. Reimplement against current protocol (stream-json reviewer command, gpt-5.5 default reviewer_model) rather than cherry-picking the stale prototype. (added 2026-04-29)

## P3 — nice to have / someday

~~Mirror the 10 stop-and-surface lint assertions to the Claude-side `skills/plan/SKILL.md` and `skills/execute/SKILL.md` so Claude-side regressions of lock-release, hallucination guards, and the git-diff failure bullet are statically caught. (added 2026-05-06)~~ (closed 2026-05-07, see 6c0dfd7 — 4 of the 10 mirrored as Claude-side `claude_*` cases (3 `contains` companions + 1 per-site `pattern_requires_adjacent`) implementing the v2.6.22 three-layer defense. The other 6 dispatch-site assertions are deliberately NOT mirrored: Claude SKILLs delegate Executor/Reviewer dispatch to `docs/protocol/planning.md` so the analogous needles are absent on Claude bodies and `pattern_requires_adjacent` would silently vacuous-pass per `tests/run_skill_lint_test.py:176-180`. Plan via review-loop:plan session 8fac7f57 (R1 REQUEST_CHANGES → R2 APPROVE, codex gpt-5.5); execute Round 1 first-pass APPROVE; lint 268→272 case-level PASS / 0 FAIL; unit tests 28/28 OK. Plugin v2.6.22 → v2.6.23.)
- Tighten B3 `tool_use_min_count` `min` from 0 to 1 on the 7 truncated smoke fixtures by addressing pre-Agent stream truncation. Today only `plan.fresh.smoke.claude` reaches an Agent dispatch within the 120s cap; the other 7 (`review-loop.regression`, `execute.from-plan`, `execute.review-only`, `execute.session-resume`, `execute.stop-after-before-polish`, `execute.stop-after-before-security`, `execute.stop-after-polish`) truncate before Agent dispatch. Three approaches: (i) trim fixture prompts so Agent dispatch happens earlier, (ii) raise per-fixture `setup.timeout_seconds` and re-measure, (iii) replace prompt with deterministic "Agent-only" smoke skipping slow planning recon. Wired vacuously today via per-fixture `{"overrides": {"min": 0, "_comment": "..."}}` per [ADR-2](DECISIONS.md). (added 2026-05-02)
- Add a dedicated `KindContainsTest` class in `tests/run_skill_lint_test.py` for explicit `kind: contains` lint coverage (needle-present → PASS, needle-absent → FAIL, missing-path → FAIL) instead of relying on the transitive integration smoke. (added 2026-05-07)
  - Note: surfaced by pr-test-analyzer during v2.6.24 Step 3.5.5 quality polish (MEDIUM, rated 6/10). Today the mechanic is exercised only via `AllSkillBodiesScopeTest` and `LintAssertionOverrideWhitelistTest`, both of which depend on real contract entries; a dedicated unit test would harden the regression case independently of the live contract.
- Harden `scripts/replay_sessions.py` error paths (silent-failure-hunter HIGH follow-ups). (added 2026-05-07)
  - Note: Two scopes: (a) `scan_file` / `build_report` should narrowly catch `OSError`/`PermissionError` per file, log to stderr with the path, surface in a new `summary.unreadable_files` count, and use a distinct exit code (e.g. 3) so I/O failures aren't conflated with the anomaly-detected exit 1; (b) test helper `run_parser` should not swallow `json.JSONDecodeError` — replace `parsed = None` fallback with an `AssertionError` that includes raw stdout/stderr, so a parser-side regression yields a clean failure message instead of a confusing downstream `TypeError`. Both items change plan-locked contracts (JSON shape + exit codes) so they require a fresh `review-loop:plan` cycle. Surfaced by silent-failure-hunter during v2.6.25 Step 3.5.3 polish — agent's verdict was APPROVE-with-noted (not blocking), so deliberately deferred.
- Expand `tests/replay_sessions_test.py` coverage for plumbing-edge gaps (pr-test-analyzer 7 MEDIUMs from v2.6.25 Step 3.5.5). (added 2026-05-07)
  - Note: 7 coverage gaps surfaced by pr-test-analyzer, all rated 5-7/10: (i) `--root` non-directory exit-code-2 path is manually verified but not asserted; (ii) no multi-file fixture — `summary.files_scanned` / `files_with_anomaly` / `total_anomaly_occurrences` aggregation is unasserted; (iii) empty-directory case (zero `*.md`) silently returns green; (iv) same-value-on-multiple-lines counts increment (plan-stated `{"general-purpose": 2}` example) is not directly locked; (v) `anomaly_sites` line-number fidelity / ordering is not asserted; (vi) JSON shape invariants (`mtime` ISO8601 format, `sort_keys=True`) not asserted; (vii) all 14 cases use subprocess+tempfile — adding a few in-process unit tests of `scan_line` / `build_report` would harden against subprocess-sandbox brittleness. Pure test additions; no plan re-approval needed.

## Done (recent, trimmed quarterly)

- ~~[partial] Expand the claude_plugin_agent_type_forbidden assertion into context-aware lint coverage.~~ (closed 2026-04-29, see 7616313 — new `forbidden_line_pattern` and `pattern_requires_adjacent` lint kinds plus `all-skill-bodies` scope; review-loop contract switches the forbidden check to a column-anchored regex and adds adjacent-CRITICAL guards for executor and code-simplifier name mentions; skills/plan/SKILL.md gains a CRITICAL marker so the new adjacency assertion holds; 8 new integration tests in tests/run_skill_lint_test.py cover the line-pattern and adjacency behaviors.)
- ~~[new] Land the Phase 3 codex-native execute split skill.~~ (closed 2026-05-01, see cbe47c7 — created `.agents/skills/plan/SKILL.md` (~313 lines) and `.agents/skills/execute/SKILL.md` (~549 lines) mirroring Claude-side sub-skills with Codex-native dispatch (`spawn_agent`/`close_agent`, fresh self-contained prompts, single-workspace constraint); both import `docs/protocol/*.md` as SSOT. Slimmed `.agents/skills/review-loop/SKILL.md` umbrella to point Planning Phase / Execution Phase narratives at the new sub-skills and added a Sub-Skills callout + Protocol Imports section. Lint contract gained 26 assertions (22 init coverage + 4 drift-hardening for lock-first ordering, provenance-aware bullet parity, Step 4 gate-failure lock release). 6 review-loop CR rounds + 3 Quality Polish agents converged with all CRITICALs resolved. Lint 261/0 PASS · 37/37 unit tests PASS. Plugin v2.6.20 released.)
- ~~Harden Codex plan/execute SKILL.md error-path documentation (silent-failure-hunter follow-ups + stop-points wording).~~ (closed 2026-05-06, see 164898d — 7 SKILL.md gaps + 10 lint assertions + Claude-side parity edits + version 2.6.21 → 2.6.22. Three-layer defense (per-site `pattern_requires_adjacent` + per-new-clause `contains` + file-level `contains`) catches stop-and-surface lock-release regressions across `.agents/skills/{plan,execute}/SKILL.md`. Lint 268 case-level PASS (baseline 258 + 10 new) · unit tests 28/28 OK. Plan via review-loop:plan session d88b0ce5 (R1-R4 + 1 typo-rerun, codex gpt-5.5 reviewer); execute round 1 APPROVE on first pass; Quality Polish 0 CRITICAL/HIGH; security preflight CLEAN.)
- ~~Add per-site `contains` companion assertions to defeat the vacuous-pass class on the 6 `pattern_requires_adjacent` stop-and-surface anchors in `tests/skills/contracts/review-loop.json`.~~ (closed 2026-05-07, see b889c1c — 6 new `kind: contains` entries (`codex_plan_executor_stop_surface_present`, `codex_plan_reviewer_claude_path_present`, `codex_plan_reviewer_codex_path_present`, `codex_execute_executor_stop_surface_present`, `codex_execute_reviewer_claude_path_present`, `codex_execute_reviewer_codex_path_present`) mirror the existing `codex_execute_git_diff_failure_present`/`_lock_release` template across both codex SKILLs. Lint baseline 272 → 278 case-level PASS / 0 FAIL; unit tests 28/28 OK. Plan via review-loop:plan session f49f5646 (R1 APPROVE first-pass with codex gpt-5.5 reviewer flagging the marketplace.json double-version-field MINOR); execute Round 1 first-pass APPROVE; Quality Polish 0 CRITICAL/HIGH (1 MEDIUM, optional `KindContainsTest` follow-up); security preflight CLEAN. Plugin v2.6.23 → v2.6.24.)
- ~~[new] Build a session-replay parser over .review-loop/sessions/*.md that reconstructs which subagent_type values were used per Agent call and flags any review-loop:* occurrences as anomalies, giving a post-hoc audit channel independent of live observation.~~ (closed 2026-05-07, see 0af3698 — `scripts/replay_sessions.py` (~190 LOC, stdlib-only) walks `.review-loop/sessions/*.md`, anchored primary regex `\bsubagent_type:` plus closed-set bare-form secondary regex over the 12 known agent names, span-overlap dedup so a single occurrence isn't double-counted; emits JSON by default (`--text` for human table); exit 1 on anomaly. 14 unittest cases (4 AC + 7 corpus-grounded with verbatim file:line citations + 1 dedup + 2 CLI). Plan via review-loop:plan session db03d9d3 (3 rounds — R2 surfaced double-count CRITICAL fixed by span-overlap dedup; R3 corrected verbatim corpus citations); execute 2 rounds (FP2 fixture verbatim fix). Quality Polish APPROVE on all 4 agents (python-reviewer ruff/mypy clean; code-reviewer 0 issues ≥80; silent-failure-hunter 2 HIGH + 4 MEDIUM noted as plumbing-edge follow-ups; pr-test-analyzer 7 MEDIUM coverage gaps as follow-ups). Real-corpus smoke: 141 files scanned, 5 anomaly files (all guard-prose by design), exit 1. Plugin v2.6.24 → v2.6.25.)
