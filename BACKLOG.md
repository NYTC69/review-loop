**Last updated**: 2026-09-19

## P0 — blocker / must-do-now

## P1 — high priority

~~Debug Claude review stall in manual / review-loop review flows.~~ (closed 2026-04-26 — root cause: `--output-format json` buffers all output until generation ends; sonnet-4-6 with extended thinking on 70k cached tokens takes 2-3 min, producing no visible output. With `--include-partial-messages` the first thinking_delta arrives at ~3.7s confirming the process is alive. Fix: switched Codex Stage 1 reviewer command to `--output-format stream-json --include-partial-messages`; orchestrator now scans line-by-line for `type == "result"` event. Updated SKILL.md, docs/protocol/planning.md, CLAUDE.md, and contract test needle.)

## P2 — normal

- ~~Reduce review-loop orchestration overhead while preserving independent review and safety. (added 2026-09-15)~~ (closed 2026-09-16 — delivered across two milestones. Proposals 1–4 shipped as v2.8.0 in `cadb06c`: content-bound evidence + invalidation (`scripts/evidence_ledger.py`), orchestrator-direct author route, bounded `## Current Review Packet` with `### Attributable Delta`, and proportional blocking review (`scripts/finding_triage.py`). Measured on 8 deterministic evaluation cases: `repeated_reads` and `unchanged_content_reviewed` both went to zero; the one reverse indicator was case 7's reviewer dispatch 2→3. That milestone grew protocol + SKILL text 3393 → 4497 lines (+33%), which proposal 5's first sub-item then repaid. Proposal 5 sub-item 1 — "load protocols by active stage" — shipped as v2.8.1 in `8dd269c`: shared `docs/protocol/loading.md` + `loading.json` contract across both runtimes' six entry skills, `scripts/read_protocol.py` exact-section loader that fails closed on missing/ambiguous/cyclic sections, entry skills reduced to thin dispatchers with `references/entry.md`, and new `parallel-review.md` / `runtime-claude.md` / `runtime-codex.md` splits. Static startup rule bytes −44–49% for plan/execute on both runtimes; small-task full-path static −14–15%. Authored by Codex session `545361dc-3d9f-4fd2-a5ea-e8da4dd466ba` under a frozen `cadb06c` control framework; independent Codex plan + code review APPROVE, plus a user-authorized independent Claude Opus delta review APPROVE (0 CRITICAL / 2 MINOR). Verified at delivery: lint 721 PASS / 0 FAIL exit 0; `pytest tests/ -q` 592 passed exit 0; security preflight clean. The remaining four proposal-5 sub-items are split out as the separate open item below.)
  - Note: Yuan requested recording these proposals for a separate follow-up session, not implementation now. Motivated by the GTO Wizard extension 1.0 release and subsequent directory migration: (1) reuse review/test evidence tied to exact content and rerun only checks invalidated by a delta, failure, or unresolved concern; (2) allow the main agent to implement small scoped changes while retaining an independent reviewer, using Executor subagents when decomposition actually helps; (3) give reviewers a compact current-requirements/diff/contracts/test-results/open-issues packet, with historical session details available on demand; (4) classify deviations from the plan by user intent, real impact, and safety rather than automatically treating a correct simplification as CRITICAL; (5) load protocols by active stage, keep current state short with append-only history, simplify prose-only replay, scope security checks to the delivery without unrelated cleanup, and evaluate risk-based use of extra adversarial review. Prioritize proposals 1–3; preserve meaningful tests, independent review, data protection, and authorization boundaries. Measure unnecessary pauses, duplicated reads/reviews/tests, elapsed time, token use, and missed defects on representative small-fix/read-only/cross-file/release tasks. These are proposals requiring review, not approval to weaken current gates. Codex entry unification is a separate local-configuration task: Yuan prefers the live development repository as the sole source, not the installed cached version.
- [new] Remaining proposal-5 orchestration-overhead sub-items, split out of the closed entry above after v2.8.1 shipped sub-item 1. Four are still open, each of which alters a separate safety or loading contract and so was deliberately excluded from both the v2.8.0 and v2.8.1 review surfaces: (a) keep current session state short with append-only history; (b) simplify prose-only replay; (c) scope Step 3.7 security checks to the delivery without unrelated cleanup; (d) evaluate risk-based use of the Step 3.4 extra adversarial review. Binding constraint carried forward from `.compass/results/2026-09-15_orchestration-overhead-guidance.md`: do NOT narrow Step 3.7, do NOT expand handsfree, and do NOT use line count to skip the Step 3.4 gate — these are proposals requiring review, not approval to weaken current gates. Codex entry unification remains a separate local-configuration task. Yuan's standing instruction (2026-09-16): this area matters to the project's direction, so schedule it when the strongest available model can do the work rather than fitting it into a cheap session. (added 2026-09-16)
- ~~[new] Agent frontmatter `tools: read-only` is not a valid Claude Code tools value; Claude Code 2.1.278 refuses to spawn `review-loop:reviewer` ("would be spawned with zero tools — unrecognized [read-only]"). 10 agents use it (reviewer, code-reviewer, comment-analyzer, pr-test-analyzer, silent-failure-hunter, type-design-analyzer, python/go/rust-reviewer, frontend-security-reviewer); `tools: all` on executor / code-simplifier is unverified. Fix: list explicit tools (e.g. `Read, Grep, Glob, Bash`) or omit the field, then spawn each agent once to confirm. (added 2026-09-19)~~ (closed 2026-09-19 — v2.8.2: `tools: all` was broken too (executor refused to spawn), so all 12 agents fixed; read-only agents now `tools: Read, Grep, Glob, Bash`, executor / code-simplifier omit `tools`. Verified: fresh headless Claude Code session spawned review-loop:reviewer and review-loop:executor, both used Read successfully; lint 721 PASS / 0 FAIL; 592 unit tests pass. Protocol still spawns via general-purpose + inlined body; docs still calling it a sandbox bug are left for a separate change.)

## P3 — nice to have / someday

~~v2.7.7 adversarial-gate 交付后 polish-tier follow-ups bundle (18 项 MEDIUM 观察).~~ (closed 2026-05-15 via v2.7.8 polish closeout — triaged all 18 observations with two parallel read-only checks, implemented/pinned the actionable subset, and intentionally did not add pthread-sigmask warning noise. Delivered: strict UTF-8 adapter input; fallback prompt strict decode + runtime-error SKIP; plugin/cache SKIP detail diagnostics + protocol lint pins; raw last-wins and multi-non-schema fixture pins; approve+critical pin; plugin-path ENOENT pin; direct SIGKILL escalation pin; adapter exit-2 fake-APPROVE stdout non-leak pin; snapshot/prompt partial tempfile cleanup on close/write/copy failures; snapshot unlink warning; module docstrings now document drain-incomplete stricter divergence and historical audit-label provenance. Verification before closeout: adapter/invoker scoped suite 91 passed, scoped ruff clean, scoped mypy clean; full-suite verification follows in this session before final.)

~~[new] Banner-parity polish-tier lint-mirror bundle (post-v2.7.3 follow-up). Two related gaps surfaced by pr-test-analyzer in session `5cf375f2-ae69-4004-9d5e-90669e3374ae`, both non-blocking, both `kind: contains` line-pin records — bundle into one delivery. (a) **Claude-side banner asymmetry** (HIGH per pr-test-analyzer): the Claude umbrella banner at `skills/review-loop/SKILL.md:217-225` has zero per-line guards while the Codex side now has 4. Add 3-4 mirror records. (b) **Codex banner row-level coverage gap** (MEDIUM per pr-test-analyzer): v2.7.3 pinned only 4 of 7 lines in `.agents/skills/review-loop/SKILL.md:73-110`. Add 4-5 records for the unpinned rows. Full inventory at `.compass/results/2026-05-10_v273-banner-parity-followup-gaps.json`. P3. (added 2026-05-10)~~ (closed 2026-05-10 via v2.7.4, plan+execute session `8e3393e9-ff1b-4c34-ae0f-4a7943abc593` — added 8 `kind: contains` records (3 Claude umbrella `:217-225` + 5 Codex umbrella `:73-110` including optional `_historical_context_row_template_declared`), updated 3 `plugin_version_pinned_*` needles `2.7.3` → `2.7.4` in lockstep, plugin v2.7.3 → v2.7.4. Plan 2 rounds (R1 RC 1 CRITICAL on 3 plugin-version-pin needles + 1 MINOR on CHANGELOG heading level → R2 APPROVE codex/gpt-5.5). Execute Round 1 first-pass APPROVE. Quality Polish: 3.5.2 SKIP (no .go/.rs/.py/.ts), 3.5.3 code-reviewer 0/0/0 + silent-failure-hunter 0/0/0, 3.5.4 simplifier 1 prose write (CHANGELOG 中文化 → fast-replay APPROVE; precedent N=3 with v2.7.2 + v2.7.3), 3.5.5 pr-test-analyzer 0/0/2 MEDIUM (non-blocking, persisted to `.compass/results/2026-05-10_v274-pr-test-analyzer-followup-observations.json`). Documentation: comment-analyzer 0/0/1 MEDIUM factual digit fix applied directly (`CHANGELOG.md:7` `3 行` → `5 行`). Security: 0 sensitive tracked files, .gitignore complete. Lint baseline 352 → 360 PASS / 0 FAIL. pytest 57/57.)

~~[new] Codex umbrella `.agents/skills/review-loop/SKILL.md` lacks the cross-runtime startup-banner that `skills/review-loop/SKILL.md:218-225` prints on the Claude side. Drift surfaced in the v2.7.2 post-delivery audit (session `1e6530e5-9b67-4e20-9266-775110854938`). Fix: mirror the `── review-loop: Starting ──` block (work item / problem / reviewer backend / mode / soft-limit) into the Codex umbrella entry-point detection, so cross-runtime users see the same launch-time UX (which backend they're on, what reviewer model). Functional workflow is unaffected — this is pure UX parity. Companion follow-ups (also surfaced by the same audit, optional bundle): explicitly document why Codex umbrella delegates Step 1.6 historical-context to the `plan` sub-skill (current placement is structurally different from Claude umbrella's inline Step 1.6 + sub-skill-resume-dedup, but end-to-end behavior is identical — 1 fetch per session); and propagate the `docs/protocol/execution.md §Step 4 Delivery Summary 中文 rendering` requirement explicitly into both `skills/execute/SKILL.md` and `.agents/skills/execute/SKILL.md` instead of leaving it as a protocol-only mandate. P3. (added 2026-05-09)~~ (closed 2026-05-10 via v2.7.3, plan session 5cf375f2-ae69-4004-9d5e-90669e3374ae — banner mirror + companion #1 historical-context delegation note + companion #2 Delivery Summary 中文 rule propagated.)

~~[new] v2.7.1 polish-tier follow-ups bundled (8 items, all surfaced during the wire-scheduler session ac3fd787, none blocking). (a) Plugin version-consistency lint. (b) Lint-protect the `runtime: "codex"` vs `runtime: "claude_code"` split in the `Parallel Reviewer Fan-Out (N>1)` subsection. (c) Lint-protect the Edit C precedence rule. (d) Lint-protect the Edit D ENOENT-as-success cleanup discipline. (e) Lint-protect the `parsed_verdict` / `parsed_issues` "best-effort metadata only" caveat. (f) Lint-protect the `_load_jobs` 10-field schema list. (g) Lint-protect the `docs/protocol/planning.md` forward-pointer anchor. (h) Root-cause and fix `review-loop.regression.smoke.claude` pre-existing fixture FAIL. (added 2026-05-09)~~ (closed 2026-05-09 via v2.7.2, session `1e6530e5-9b67-4e20-9266-775110854938`. Plan via review-loop:plan 4 rounds (R1 RC 4 CRITICAL, R2 RC 3 CRITICAL on b/c/d needles, R3 RC 1 CRITICAL on c1 line-wrap, R4 APPROVE codex/gpt-5.5). Execute Round 1 first-pass APPROVE; Round 2 RC 1 CRITICAL on item-h plan deviation A→B (plan acceptance-umbrella argument accepted); Round 2 APPROVE. Quality Polish: code-reviewer 0/0/0, silent-failure-hunter 0/0/0, code-simplifier 1 prose fix (replay→APPROVE), pr-test-analyzer 0/0/4 MEDIUM (P3 polish-tier follow-ups, non-blocking). Documentation Consistency: comment-analyzer 0/2 HIGH factual fixes applied directly in smoke/README.md. Security Preflight: 0 sensitive files, .gitignore complete. Delivered as 48 inline `kind: contains` lint records (a=3, b=9, c=12, d=9, e=4, f=10, g=1) + 1 smoke assertion + group member (item h Option A retained as harmless extension) + new `tests/skills/smoke/README.md` documenting `best_effort` flake (item h Option B; root cause is `claude -p` 600s timeout, not heading drift the plan diagnosed). Lint baseline 297 → 345 PASS / 0 FAIL. tests/review_verification_test.py 57/57. Plugin v2.7.1 → v2.7.2.)

~~Mirror the 10 stop-and-surface lint assertions to the Claude-side `skills/plan/SKILL.md` and `skills/execute/SKILL.md` so Claude-side regressions of lock-release, hallucination guards, and the git-diff failure bullet are statically caught. (added 2026-05-06)~~ (closed 2026-05-07, see 6c0dfd7 — 4 of the 10 mirrored as Claude-side `claude_*` cases (3 `contains` companions + 1 per-site `pattern_requires_adjacent`) implementing the v2.6.22 three-layer defense. The other 6 dispatch-site assertions are deliberately NOT mirrored: Claude SKILLs delegate Executor/Reviewer dispatch to `docs/protocol/planning.md` so the analogous needles are absent on Claude bodies and `pattern_requires_adjacent` would silently vacuous-pass per `tests/run_skill_lint_test.py:176-180`. Plan via review-loop:plan session 8fac7f57 (R1 REQUEST_CHANGES → R2 APPROVE, codex gpt-5.5); execute Round 1 first-pass APPROVE; lint 268→272 case-level PASS / 0 FAIL; unit tests 28/28 OK. Plugin v2.6.22 → v2.6.23.)
- Jev triage: independent second opinion on whether a Reviewer [CRITICAL] is over-engineering (3-phase validation). (added 2026-09-19)
  - Note: evaluate TypeSafe's Jev System One model as an independent second opinion on whether a Reviewer [CRITICAL] is over-engineering (unreachable / never-triggered-in-normal-use corner case) and should stop blocking a review-loop round; for [MINOR], a three-way route (fix now / BACKLOG / drop) that never opens a new round. Three-stage validation: Phase 0 offline replay kill-screen over historical session findings (plan reviewer-approved in session `2fc72a78-12a6-4b29-acbc-74146d268180`, 3 rounds, Opus subagent reviewer; throwaway spike, no tracked changes; 20 blind user labels; pre-registered KILL / INSUFFICIENT_LEVERAGE / INCONCLUSIVE / HAIKU_WINS_OR_TIE / GO / GO_ARM_UNDETERMINED / NO_GO verdicts; can only kill, not validate); Phase 1 shadow mode (record Jev verdicts, act on none) — needs its own full plan review; Phase 2 act mode only after pre-declared shadow criteria are met. Binding constraints: protected categories (security, data loss, authorization, validated-behavior regression) and spec-or-plan-gap findings are never downgradable; downgraded findings stay visible to the user with Jev's probabilities; the optional integration must fail silently on both runtimes and be off by default (diff / finding text egress to a third-party API); this is a gate-weakening proposal requiring review, not approval to weaken current gates — do NOT narrow Step 3.7, do NOT expand handsfree, do NOT use line count to skip Step 3.4. Must also beat a cheap-LLM judge (Haiku) arm or there is no reason to add the dependency. Results land in `.compass/results/2026-09-19_jev-triage-phase0.json`.
  - Progress 2026-09-19 — Phase 0 done, verdict `INSUFFICIENT_LEVERAGE` (also `INCONCLUSIVE`); demoted P2 -> P3 by Yuan, kept open. Under the pre-registered policy Jev would downgrade 0/151 historical blocking CRITICALs and Haiku 0/151 (bar: 5%). Dominant blocker: q1 "triggers under normal use" >= 0.15 for 148/151 (Jev) and 151/151 (Haiku); `spec-or-plan-gap` is 41% (Jev) / 61% (Haiku) of the pool and is non-downgradable by ruling. The underlying observation still holds: two independent AI labelers (Codex + Opus; the owner could not label — context lost) put the over-engineering base rate at 3/20 by consensus (95% CP 3.2-37.9%), 7/20 by either, with kappa 0.47 — the concept's boundary is fuzzy even for strong models. Boundary: AI-consensus ground truth, offline replay, almost no six-field-format findings in history; nothing here estimates live savings. Do NOT enter Phase 1 on this spec. If retried: change the question (ask for the concrete bad outcome under normal use, not "would it trigger"), use post-v2.8.0 six-field findings as input, and reuse the 20 double-labeled cards as a ready eval set; a cheaper first step is to check whether CRITICAL counts / rounds dropped after v2.8.0. Report: `.compass/results/2026-09-19_jev-triage-phase0-report.md`; data: `.compass/results/2026-09-19_jev-triage-phase0.json`.

## Done (recent, trimmed quarterly)

- ~~[new] Verify the live full-workflow reduction that v2.8.1 claims only statically. `scripts/measure_protocol_loading.py` reports a static UTF-8 instruction-byte inventory, explicitly excluding agent bodies and task packets from both columns; the target of ≥25% real small-task rule-input reduction, plus token/cost/wall-clock improvement, is UNVERIFIED. Needs an observed before/after capture of a real workflow on a native runtime — not a simulated reading exercise and not a re-reading of the static table. Until then, no delivery summary may present the static numbers as live workflow savings.~~ (closed 2026-09-17 — 首次取得真实 native-runtime 的规则输入实测。载体是 compass 仓库的真实 work item（删除 `commands/` 适配层），Claude Code runtime，plan 2 轮 + execute 1 轮 + 一次 orchestrator-direct 修复，29 个文件改动，约 70 分钟、6 个 subagent、816k subagent token。按 `tasks/review-loop-protocol-slimming-plan.md` L114-118 的口径逐次投递记账（**含** agent body 与 task packet —— `scripts/measure_protocol_loading.py` 把这两项从两列都排除了）：**AFTER 实测 166,644 B（orchestrator 上下文 114,837 + fresh-agent 投递 51,807）vs BEFORE 反事实 233,780 B = −28.7%,达标 ≥25%**。其中 27,427 B 是 Codex 配额触顶后作废的 reviewer prompt,照实计入 AFTER;扣除后为 −40.4%。**测量边界（引用时必须带上）：这是 observed-AFTER 对 reconstructed-BEFORE,不是完整的 observed 双列。** AFTER 列是真实运行的逐次投递记账 —— 这正是本条要的、此前从不存在的那一半;BEFORE 列仍取 `cadb06c` 的静态 inventory（claude / one-file-delivery 全路径 181,973 B),因为产出 observed BEFORE 需要把同一 work item 在 v2.8.0 入口技能下重跑一遍 —— 第二次约 70 分钟、约 80 万 token,换一个主要用于回溯的数字,判为不值。另:分母用的 one-file-delivery 场景比本轮轻（本轮多一轮 planning 和一次直写修复),所以 28.7% 是保守下界。最有价值的一条是静态表测不出来的:**planning-review bundle 在 planning 第 2 轮重载时投递 0 字节** —— 15 个单元指纹全部仍在上下文中被跳过,而该 bundle 全量 54,612 B;跨轮指纹去重才是 loader 的主要收益来源。同轮实测:必要规则漏加载 = 0,未预加载任何后续 stage（gate/polish/docs/security/delivery 全程未载入);`stale_evidence_reused` = 0,`blocking_rejected_or_downgraded` = 0,`theoretical_complexity_added` = 0。注意 reviewer backend 因配额全程走 subagent fallback(opus),codex reviewer 路径会改变 fresh-agent 列但不影响 orchestrator 列。完整数据:`.compass/results/2026-09-17_v2.8.1-live-workflow-measurement.json`。产出该数据的 session:`~/3Cats/compass/.review-loop/sessions/cbafc704-5c38-4d3f-993e-85700d8fd526.md`。)
- ~~[new] Bring back a conflict-aware parallel CR scheduler (`scripts/review_verification.py` + wrapper + tests). The pruned `feat/lint-agent-type-context-guard` branch (ref via git reflog) prototyped one in mid-April: a per-job scheduler that runs review-loop CRs in parallel under a strict per-job timeout, with returncode propagation, drain-on-timeout, and immutability of finished jobs. Useful when one orchestrator wants to fan out N independent reviewer rounds without serial wall-clock cost. Current main has no such scheduler — every CR is sequential. Reimplement against current protocol (stream-json reviewer command, gpt-5.5 default reviewer_model) rather than cherry-picking the stale prototype. (added 2026-04-29)~~ (closed 2026-05-08, see 01a0401 — v2.7.0 milestone — `scripts/review_verification.py` (716 lines, stdlib-only) + `tests/review_verification_test.py` (57 cases / 16 classes) + 3 markdown forward-pointer edits. Two-mechanism design: binary conflict locks (codebase invariants only — `prompt_file:{session_id}:{job_id}` + opt-in `worktree:{path}`; NO `cli_rate:*` in default) plus opt-in capacity counters (`cli_rate:claude` / `cli_rate:codex`). `_run_one` uses POSIX file-FD handoff (`Popen(stdin=open(prompt_path, "rb"))` not `stdin=PIPE`) with `os.killpg(os.getpgid(...), SIGTERM/SIGKILL)`, bounded `proc.wait(timeout=5.0)` post-kill, snapshot-once finalization for late-drain immutability, and `finally:` cleanup of FD + temp file. `_validate_reviewer_output_schema` strictly enforces `docs/protocol/reviewer-output.md` (verdict allowlist, required `### Strengths`, bullet-prefix issue parsing, severity allowlist, six `schema_violation:*` discriminators). Plan via review-loop:plan session `f1c0fa2f-2371-46d8-92cd-d02302b174b2` (R1 RC default `cli_rate:*` → R2 RC contract citation drift → R3 RC subprocess stdin gap → R4 APPROVE codex/gpt-5.5); execute R1 RC LateDrain test theater + ParallelFanOut bypass + parser schema laxity → R2 RC unknown-severity silent ignore → R3 APPROVE; Quality Polish 1 fix batch (claim-revert atomicity + bounded post-kill wait + reader sentinel + close() exception broadening + `os.killpg(getpgid)` + 4 simplifier extractions). 39 → 51 → 53 → 57 unit tests, all PASS. Lint baseline 288 PASS / 0 FAIL preserved; ruff clean; smoke `exit 0` (2 pre-existing fixture FAILs unrelated). Plugin v2.6.33 → v2.7.0. Orchestrator wiring at 3 Codex Stage 1 reviewer-dispatch sites is a separate spawned P2 follow-up. Final-boss BACKLOG P2 item completed.)
- ~~[new] Wire scheduler into Codex Stage 1 reviewer dispatch (3 sites: `.agents/skills/review-loop/SKILL.md:378`, `.agents/skills/plan/SKILL.md:224`, `.agents/skills/execute/SKILL.md:342`). Library + tests + forward-pointers landed via session `f1c0fa2f-2371-46d8-92cd-d02302b174b2`; this follow-up replaces the single-shot `claude -p` shell-out at each anchor with a `Scheduler.submit([...])` call when the caller fans out, preserving single-shot when N=1. P2. (added 2026-05-08)~~ (closed 2026-05-09, see 87fa729 — v2.7.1 — three SKILL files (`.agents/skills/{review-loop,plan,execute}/SKILL.md`) each gained a `Parallel Reviewer Fan-Out (N>1)` subsection (peer `###` for review-loop, nested `####` for plan/execute) teaching orchestrators to keep N=1 byte-identical with the existing `claude -p` shell-out and shell out to `python3 scripts/review_verification.py --jobs <path> --output <path>` when N>1. Per-job prompt path standardized as `{session_id}-reviewer-prompt.{job_id}.txt` matching `scripts/review_verification.py:457-459` / `:648-651`. `runtime: "codex"` jobs surface stdout via `subprocess.PIPE` through `<results.json>` `stdout`; `runtime: "claude_code"` jobs use the per-job output file. Edit C added precedence rule (`error` / `timed_out` / `returncode != 0` → command-execution failure, skip stream-json parse for that entry); Edit D added cleanup discipline (scheduler-owned prompt files ENOENT-as-success, orchestrator-owned `<jobs.json>` / `<results.json>` non-ENOENT failures logged as non-blocking warnings). `docs/protocol/planning.md` forward pointer rewritten from "wiring lands in a follow-up" to concrete anchor list. 9 inline `kind: contains` lint records added to `tests/skills/contracts/review-loop.json` (3 needles × 3 files): heading anchor, scheduler invocation, per-job prompt path; lint baseline 288 → 297 PASS / 0 FAIL. CLAUDE.md Codex Stage 1 Notes updated. Plan via review-loop:plan session `ac3fd787-6c93-43ea-8b91-0a54e7ed0028` (2 rounds — R1 RC 3 CRITICAL: wrong per-job path format, lint registry vs runnable confusion, unsupported `kind: needle`; R2 APPROVE codex/gpt-5.5); execute R1 RC config.md scope creep + runtime-branch wording → R2 APPROVE; Polish 3.5.3 silent-failure-hunter HIGH+MEDIUM → Edit C/D fix → APPROVE; pr-test-analyzer 7 MEDIUM polish-tier follow-ups noted (plugin version-consistency lint, runtime split protection, Edit C/D protection, `parsed_verdict` caveat protection, `_load_jobs` field-list protection, forward-pointer protocol-side protection). Step 3.6 docs replay deviation: CLAUDE.md alignment is metadata-only doc clarification, full replay compressed to single exec-mode reviewer pass which APPROVED. Security preflight clean. Smoke 1 FAIL (`review-loop.regression.smoke.claude`) pre-existing fixture flake unrelated to this docs/contract change. Plugin v2.7.0 → v2.7.1.)
- ~~Align protocol with `L-review-loop-simplifier-prose-replay-precedent` LEARNING (LEARNINGS.md:34) so `docs/protocol/execution.md` Step 3.6 + `docs/protocol/session-file.md` `completed_stages` lifecycle do not contradict the established N=3 prose-only fast-replay practice. Currently the protocol still says "any Step 3.6 write clears `completed_stages` and replay restarts from `exec`", while the LEARNING says reviewer-only fast-replay (no Executor re-dispatch) is correct when (a) the write is prose / digit / metadata only, (b) it touches no `tests/skills/contracts/*.json` lint-pinned needle, and (c) `bash scripts/run-skill-lint` count is unchanged. Codex review of v2.7.5 (session 2026-05-10) flagged this as a P2 because future review-loop runs will see conflicting instructions. Fix: amend Step 3.6 + replay lifecycle text with an explicit "prose-only / no-lint-needle / no-baseline-change" exception clause that points back to the LEARNING anchor. Also propagate the matching wording to `skills/execute/SKILL.md` and `.agents/skills/execute/SKILL.md` so lint can statically guard the exception.~~ (closed 2026-05-10, see 9a5f413 — v2.7.6 reviewer-only fast-replay protocol alignment shipped and pushed.)
- ~~加一轮终局 adversarial-review gate (CLI reviewer APPROVE 之后跑一次 codex adversarial,双端对称).~~ (closed 2026-05-14, see 872c1af — Delivered as v2.7.7. 6-round plan loop + 3-round exec + Step 3.5/3.6/3.7 quality polish. 2 rounds of meta-dogfood adversarial-review caught 5 real bugs that 9+ rounds of protocol-aware reviewer missed (validating the core hypothesis of this feature): plugin-json envelope unwrap, blocking-findings authoritative rendering, unconditional SIGKILL on cached pgid, fallback cleanup deletes codex-bootstrap-created config, drain-incomplete kills pgroup. 18 polish-tier MEDIUM observations filed as separate P3 v2.7.8 follow-up. 39 new adversarial-gate tests, lint baseline 370 → 382 (+12 kind:contains needles), 0 regression on existing 187 tests.)
- ~~v2.7.7 adversarial-gate 第 3 次 meta-dogfood 验证 (运行后看 finding 数量再决定是否打到 v2.7.8 bundle 里).~~ (closed 2026-05-15, see 783f189 — superseded by final single-pass Step 3.4 policy: no further adversarial self-loop / R3 meta-dogfood pass is needed for convergence; gate REQUEST_CHANGES now feeds ordinary Step 3 Executor/Reviewer repair rounds, and a later normal reviewer APPROVE mints `exec`.)
  - Note: 原 follow-up 要求再跑一次 R3 adversarial-review 验证 amended v2.7.7。该判断已被用户校准取代：Terminal Adversarial Gate 每个 execution convergence 只跑一轮，发现的问题交给普通 review-loop 收敛。
