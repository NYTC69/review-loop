**Last updated**: 2026-05-08 (v2.7.0 close P2 parallel CR scheduler)

## P0 — blocker / must-do-now

## P1 — high priority

~~Debug Claude review stall in manual / review-loop review flows.~~ (closed 2026-04-26 — root cause: `--output-format json` buffers all output until generation ends; sonnet-4-6 with extended thinking on 70k cached tokens takes 2-3 min, producing no visible output. With `--include-partial-messages` the first thinking_delta arrives at ~3.7s confirming the process is alive. Fix: switched Codex Stage 1 reviewer command to `--output-format stream-json --include-partial-messages`; orchestrator now scans line-by-line for `type == "result"` event. Updated SKILL.md, docs/protocol/planning.md, CLAUDE.md, and contract test needle.)

## P2 — normal

~~[new] Add a dry-run Orchestrator mode that executes /review-loop against a fixture repo and validates the Agent-call sequence without writing files, so sandbox/agent-type bugs are caught pre-merge instead of via live tool_uses: 0 symptoms. (added 2026-04-19)~~ (closed 2026-05-02 — refined to lint+smoke assertions per [ADR-1](DECISIONS.md). No orchestrator process exists to dry-run; SKILL.md IS the orchestrator-as-prose. Replaced with 4-gap concrete scope: lint kinds A1 `command_flag_co_occurrence` + A2 `agent_subagent_type_whitelist`, smoke kinds B3 `tool_use_min_count` + B4 `tool_use_agent_subagent_type_whitelist`. Plan approved over 4 codex-reviewed rounds; see `.review-loop/sessions/b1e5ecca-6bc1-43cb-b6d3-c8b5174e60ca.md`. Implementation pending via `review-loop:execute --session b1e5ecca-...`.)
- [new] Wire scheduler into Codex Stage 1 reviewer dispatch (3 sites: `.agents/skills/review-loop/SKILL.md:378`, `.agents/skills/plan/SKILL.md:224`, `.agents/skills/execute/SKILL.md:342`). Library + tests + forward-pointers landed via session `f1c0fa2f-2371-46d8-92cd-d02302b174b2`; this follow-up replaces the single-shot `claude -p` shell-out at each anchor with a `Scheduler.submit([...])` call when the caller fans out, preserving single-shot when N=1. P2. (added 2026-05-08)

## P3 — nice to have / someday

~~Mirror the 10 stop-and-surface lint assertions to the Claude-side `skills/plan/SKILL.md` and `skills/execute/SKILL.md` so Claude-side regressions of lock-release, hallucination guards, and the git-diff failure bullet are statically caught. (added 2026-05-06)~~ (closed 2026-05-07, see 6c0dfd7 — 4 of the 10 mirrored as Claude-side `claude_*` cases (3 `contains` companions + 1 per-site `pattern_requires_adjacent`) implementing the v2.6.22 three-layer defense. The other 6 dispatch-site assertions are deliberately NOT mirrored: Claude SKILLs delegate Executor/Reviewer dispatch to `docs/protocol/planning.md` so the analogous needles are absent on Claude bodies and `pattern_requires_adjacent` would silently vacuous-pass per `tests/run_skill_lint_test.py:176-180`. Plan via review-loop:plan session 8fac7f57 (R1 REQUEST_CHANGES → R2 APPROVE, codex gpt-5.5); execute Round 1 first-pass APPROVE; lint 268→272 case-level PASS / 0 FAIL; unit tests 28/28 OK. Plugin v2.6.22 → v2.6.23.)

## Done (recent, trimmed quarterly)

- ~~[new] Build a session-replay parser over .review-loop/sessions/*.md that reconstructs which subagent_type values were used per Agent call and flags any review-loop:* occurrences as anomalies, giving a post-hoc audit channel independent of live observation.~~ (closed 2026-05-07, see 0af3698 — `scripts/replay_sessions.py` (~190 LOC, stdlib-only) walks `.review-loop/sessions/*.md`, anchored primary regex `\bsubagent_type:` plus closed-set bare-form secondary regex over the 12 known agent names, span-overlap dedup so a single occurrence isn't double-counted; emits JSON by default (`--text` for human table); exit 1 on anomaly. 14 unittest cases (4 AC + 7 corpus-grounded with verbatim file:line citations + 1 dedup + 2 CLI). Plan via review-loop:plan session db03d9d3 (3 rounds — R2 surfaced double-count CRITICAL fixed by span-overlap dedup; R3 corrected verbatim corpus citations); execute 2 rounds (FP2 fixture verbatim fix). Quality Polish APPROVE on all 4 agents (python-reviewer ruff/mypy clean; code-reviewer 0 issues ≥80; silent-failure-hunter 2 HIGH + 4 MEDIUM noted as plumbing-edge follow-ups; pr-test-analyzer 7 MEDIUM coverage gaps as follow-ups). Real-corpus smoke: 141 files scanned, 5 anomaly files (all guard-prose by design), exit 1. Plugin v2.6.24 → v2.6.25.)
- ~~Expand `tests/replay_sessions_test.py` coverage for plumbing-edge gaps (pr-test-analyzer 7 MEDIUMs from v2.6.25 Step 3.5.5).~~ (closed 2026-05-08, see dadb537 — 15 new test methods in 3 new classes (`PlumbingEdgeTest` 7 + `ScanLineUnitTest` 5 + `BuildReportUnitTest` 3) close all 7 MEDIUM gaps verbatim. Plan via review-loop:plan session 4c287811 (R1 REQUEST_CHANGES on `summary.mtime` Context typo + full-key-list + count-prose; R2 APPROVE with codex gpt-5.5 reviewer); execute Round 1 first-pass APPROVE; replay round after simplifier APPROVE (codex confirmed assertion-power-preserving subset). Quality Polish APPROVE on all 4 agents (python-reviewer ruff PASS / mypy 1 import-resolution warning / bandit test-context FPs / pip-audit unavailable; code-reviewer 0 ≥80; silent-failure-hunter 0; pr-test-analyzer 6 MEDIUM second-tier coverage follow-ups). Test count 14 → 29 OK; lint baseline 288 PASS / 0 FAIL preserved. Plugin v2.6.25 → v2.6.26.)
- ~~Add a dedicated `KindContainsTest` class in `tests/run_skill_lint_test.py` for explicit `kind: contains` lint coverage.~~ (closed 2026-05-08, see 11323b6 — bundled with second-tier replay_sessions sweep into v2.6.28; new `KindContainsTest` class with 3 methods isolates needle-present/absent/missing-path branches via `LintAssertionFixtureCase` subprocess pattern.)
- ~~Second-tier `tests/replay_sessions_test.py` coverage tightening (pr-test-analyzer 6 MEDIUMs from v2.6.26 Step 3.5.5).~~ (closed 2026-05-08, see 11323b6 — `SecondTierCoverageTest` class added with 7 methods covering 6 gaps verbatim (gap 1 split into sq + dq sub-tests). Plan via review-loop:plan session 1acb3e6e (R1 REQUEST_CHANGES on `record["reason"]` vs stdout assertion target + Gap 5 dedup-formula recompute; R2 APPROVE codex gpt-5.5); execute Round 1 first-pass APPROVE; replay × 2 (after simplifier write_fixture consistency + after docs-stage docstring fix). Quality Polish APPROVE on all 4 agents; pr-test-analyzer surfaced 6 third-tier MEDIUMs filed as new P3. Test count 57 → 67 (+10 new) across both test files. Plugin v2.6.27 → v2.6.28.)
- ~~[new] Bring back a conflict-aware parallel CR scheduler (`scripts/review_verification.py` + wrapper + tests). The pruned `feat/lint-agent-type-context-guard` branch (ref via git reflog) prototyped one in mid-April: a per-job scheduler that runs review-loop CRs in parallel under a strict per-job timeout, with returncode propagation, drain-on-timeout, and immutability of finished jobs. Useful when one orchestrator wants to fan out N independent reviewer rounds without serial wall-clock cost. Current main has no such scheduler — every CR is sequential. Reimplement against current protocol (stream-json reviewer command, gpt-5.5 default reviewer_model) rather than cherry-picking the stale prototype. (added 2026-04-29)~~ (closed 2026-05-08, see 01a0401 — v2.7.0 milestone — `scripts/review_verification.py` (716 lines, stdlib-only) + `tests/review_verification_test.py` (57 cases / 16 classes) + 3 markdown forward-pointer edits. Two-mechanism design: binary conflict locks (codebase invariants only — `prompt_file:{session_id}:{job_id}` + opt-in `worktree:{path}`; NO `cli_rate:*` in default) plus opt-in capacity counters (`cli_rate:claude` / `cli_rate:codex`). `_run_one` uses POSIX file-FD handoff (`Popen(stdin=open(prompt_path, "rb"))` not `stdin=PIPE`) with `os.killpg(os.getpgid(...), SIGTERM/SIGKILL)`, bounded `proc.wait(timeout=5.0)` post-kill, snapshot-once finalization for late-drain immutability, and `finally:` cleanup of FD + temp file. `_validate_reviewer_output_schema` strictly enforces `docs/protocol/reviewer-output.md` (verdict allowlist, required `### Strengths`, bullet-prefix issue parsing, severity allowlist, six `schema_violation:*` discriminators). Plan via review-loop:plan session `f1c0fa2f-2371-46d8-92cd-d02302b174b2` (R1 RC default `cli_rate:*` → R2 RC contract citation drift → R3 RC subprocess stdin gap → R4 APPROVE codex/gpt-5.5); execute R1 RC LateDrain test theater + ParallelFanOut bypass + parser schema laxity → R2 RC unknown-severity silent ignore → R3 APPROVE; Quality Polish 1 fix batch (claim-revert atomicity + bounded post-kill wait + reader sentinel + close() exception broadening + `os.killpg(getpgid)` + 4 simplifier extractions). 39 → 51 → 53 → 57 unit tests, all PASS. Lint baseline 288 PASS / 0 FAIL preserved; ruff clean; smoke `exit 0` (2 pre-existing fixture FAILs unrelated). Plugin v2.6.33 → v2.7.0. Orchestrator wiring at 3 Codex Stage 1 reviewer-dispatch sites is a separate spawned P2 follow-up. Final-boss BACKLOG P2 item completed.)
